# YouDaoNoteLM 开发规范

## 这是什么项目

YouDaoNoteLM 是面向真实用户的 NotebookLM 风格知识工作台：用户导入自己的笔记、文件、网页和音频，系统将其转换为可检索知识，并提供带引用的问答、流式对话和内容生成。它不是演示项目；用户数据、账号安全、检索可信度、费用可控性和可恢复性优先于新增功能的速度。

Agent Harness 是支撑 Chat/Main/Search 等 Agent 能力的通用运行基础，不是独立产品，也不应改变既有领域任务的所有权。涉及其具体语义时，以 `docs/superpowers/specs/2026-07-28-project-agent-harness-architecture.md` 为准；本文件只定义如何安全、可维护地开发本项目。

## 技术与运行约定

- 后端使用 Go 1.25、Gin、GORM 和 MySQL；前端使用 TypeScript、React 19、Vite 和 Tailwind。Python 仅用于 MarkItDown 服务及独立脚本，不能被引入为 Go 服务的运行时依赖。
- 生产部署以 Docker Compose 为基线。MySQL 是业务数据事实源；Redis 是缓存、会话、限流或通知加速器；Milvus 是向量检索；MinIO 保存对象。不要因局部方便把关键业务事实迁入缓存或进程内状态。
- 默认保持模块化单体与现有 API 兼容。新服务、消息队列、数据库、全局框架或大型依赖必须先有明确的产品/容量/运维理由，不能为了“可扩展”预埋。
- 代码、配置和文档中的版本约束是兼容性边界。不得无关升级 Go、Eino、React、数据库镜像或 lockfile；确有需要时必须单独说明风险、升级路径和验证范围。
- Harness建设中的分支功能查看docs/plan/harness/branch_design.md，必须查看后再修改

## 产品与数据红线

- 所有读取、写入、下载、删除、异步任务和流式接口都必须在服务端验证用户身份及对 conversation、notebook、document、artifact 等资源的所有权。客户端传来的 ID 不是授权依据。
- 绝不提交、回显或记录真实密码、JWT、Cookie、API Key、数据库 DSN、MinIO 凭据、加密密钥、用户正文、完整 Prompt、原始网页内容或 checkpoint。示例配置只能使用明显的占位符；新增敏感配置必须写入 `.env.example` 或相应 example 配置，并说明用途。
- 不绕过 API Key 加密、密码哈希、鉴权、限流、验证码或审计逻辑来方便测试。测试应使用 fake、fixture 或专用临时凭据。
- 不以自动重试掩盖具有外部副作用的失败。对邮件、第三方写操作、文件删除、导入、生成或 Agent 工具调用，先明确幂等键、超时、取消行为和失败后的用户可见状态。
- 不做不可逆的数据操作：不得在代码、迁移、脚本或调试命令中静默删库、清空用户文件、批量重建向量或删除对象。数据迁移、清理和 backfill 必须有目标范围、备份/回滚方案及用户批准。
- 不将 Redis、SSE/WebSocket 连接、HTTP request context、goroutine 或内存 map 作为需要跨重启保持正确性的唯一状态源。

## 兼容性与变更纪律

- 先选择能完成需求的最小改动。不要在同一个任务中顺带重构、重命名、格式化全仓、替换库、改变无关 API 或清理历史代码。
- 默认保持已有 HTTP API、JSON 字段、错误码、SSE 事件、数据库数据和配置键兼容。若必须破坏兼容性，先明确受影响客户端/数据、迁移或双读写策略、回滚方式和发布日期；没有这些信息时先询问。
- Schema 变更必须是显式、可审查的迁移，且要考虑已有数据、索引、锁表时间、前进迁移与回滚。不要把 `AutoMigrate` 或应用启动逻辑当成生产数据迁移方案。
- Handler 只处理 HTTP/SSE 协议、输入校验和响应映射；业务规则属于 service/use-case；持久化属于 repository/adapter。不要让 repository 反向依赖 service，也不要把业务流程藏进 handler、model hook 或配置加载代码。
- 库代码返回带上下文的错误，不调用 `panic`、`log.Fatal` 或 `os.Exit`。启动入口可 fail-fast 配置错误，但必须给出不泄露秘密的可诊断信息。
- 遵守 `context.Context` 的取消和 deadline。任何刻意脱离父 context 的后台工作都必须有明确 owner、持久化状态、超时、清理与可观测结果；不能只因“避免被取消”而使用 `context.Background()` 或 `context.WithoutCancel()`。
- 前端不得把权限判断、密钥保护或数据完整性寄托在 UI；后端才是权威。涉及加载、空态、失败、重试或流式断连的 UI 改动，必须保留可理解的用户反馈。

## 代码质量

- Go 改动必须保持 `gofmt` 风格；优先清晰的领域类型、小函数和显式依赖。不要为一次使用创建抽象层、service locator、全局可变单例或“万能” interface。
- TypeScript 改动保持类型安全；不要用 `any`、静默 catch、类型断言或忽略 lint 来掩盖接口不确定性。共享 API 类型变化要同步检查前后端契约。
- 命名要表达领域含义，避免 `util`、`helper`、`manager`、`data` 等无边界包或类型。导出的 Go API、复杂并发/一致性逻辑和反直觉取舍应说明“为什么”，不要重复代码表面行为。
- 日志应可排障但不泄密：记录稳定的操作、资源类型、受控 ID、错误分类和必要上下文；不得把高基数用户 ID/Run ID 当 metrics label。
- 新配置必须有安全默认值、验证、example 文档和向后兼容策略。不要为了测试把 debug、宽 CORS、跳过 TLS、跳过鉴权或危险开关默认打开。

## 测试与验证

每次改动按风险选择最窄而充分的验证，并在交付时报告实际命令与结果：

1. Go 逻辑、并发、鉴权、错误路径、数据映射或修复：先新增/更新相邻 `*_test.go`，至少运行受影响包的 `go test`；涉及共享路径时运行 `go test ./...`。涉及共享可变状态、goroutine 或取消时运行 `go test -race` 覆盖相关包，条件允许时使用 CI 同等命令 `go test -race -coverprofile=coverage.out ./...`。
2. Go 编译面、接口或导入变更：运行 `go vet ./...`，并确保受影响服务能构建（例如 `go build ./cmd/server`）。
3. 前端 TypeScript/React 改动：在 `frontend` 运行 `npm run lint` 与 `npm run build`。涉及交互、响应式布局或流式状态时，还要在可运行环境中人工验证关键路径；项目没有现成前端测试脚本时，不要伪称已运行单元测试。
4. Schema、repository、配置或外部适配器改动：除单测外，在隔离的开发环境验证迁移和最小健康路径。不得用真实用户库、生产 API Key 或生产对象存储做测试。
5. Docker、部署或依赖服务改动：构建相关镜像/服务并检查 `/api/v1/health`，同时说明未能在本地验证的外部依赖。

测试应覆盖可观察的行为和失败路径，而不只是实现细节。修复缺陷时，能复现就先写失败测试；无法复现时，要说明原因和用于防回归的替代验证。测试失败要区分本次回归、既有失败和环境限制，绝不删除或放宽测试来获得通过。

## 工作方式与交付

- 动手前说明会影响结果的假设、成功标准和有意义的取舍。需求在权限、数据迁移、公开 API、用户体验或外部副作用上有歧义时，先请求澄清。
- 开始前检查工作树；保留用户已有的未提交改动。不要使用破坏性 Git 或文件操作来获得干净状态。
- 修改范围必须与任务直接相关。发现不相关问题时记录给用户，不要顺手修复。
- 文档、示例配置、迁移说明与运维说明是功能的一部分：当改动会改变使用方式、配置、数据或运行行为时同步更新。
- 最终交付简要说明：改了什么、为什么、已运行的验证及结果、未验证或仍有的风险。没有最新验证证据，不要声称完成。
